import { describe, it, expect } from 'vitest';

describe('Hay School Backend', () => {
  it('should be able to run tests', () => {
    expect(true).toBe(true);
  });

  it('should have basic functionality', () => {
    expect(1 + 1).toBe(2);
  });
});
